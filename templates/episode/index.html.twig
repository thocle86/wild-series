{% extends 'base.html.twig' %}

{% block title %}Liste des épisodes{% endblock %}

{% block body %}
    <h1>Liste des épisodes</h1>
    <a href="{{ path('episode_new') }}" class="navigation">Créer un nouvel épisode</a>

    <table class="table">
        <thead>
            <tr>
                <th>Série</th>
                <th>Saison</th>
                <th>Titre</th>
                <th>Numéro</th>
                <th>Synopsis</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for episode in episodes %}
            <tr>
                <td>{{ episode.season.program.title }}</td>
                <td>{{ episode.season.number }}</td>
                <td>{{ episode.title }}</td>
                <td>{{ episode.number }}</td>
                <td>{{ episode.synopsis }}</td>
                <td>
                    <a href="{{ path('episode_show', {'id': episode.id}) }}">show</a>
                    <a href="{{ path('episode_edit', {'id': episode.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucunes données trouvées</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
