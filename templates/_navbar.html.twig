<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('index') }}">
        <img src="{{ asset('build/logo-tv.png') }}" width="60" max-height="100%" alt="">
        <h1 class="logo_title">Wild Series</h1>
    </a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto navbar-acces-pages">
            <!--CATEGORIES-->
            <li class="nav-item">
                <a class="nav-link" href="{{ path('category_index') }}">Catégories</a>
            </li>
            <!--SERIES-->
            <li class="nav-item">
                <a class="nav-link" href="{{ path('program_index') }}">Séries</a>
            </li>
            <!--ACTEURS-->
            <li class="nav-item">
                <a class="nav-link" href="{{ path('actor_list') }}">Acteurs</a>
            </li>
            <!--MES FAVORIS-->
            <li class="nav-item">
                <a class="nav-link" href="#">Mes favoris</a>
            </li>
        </ul>
        <ul class="navbar-nav mr-auto navbar-connexion">
            {% if not app.user %}
            <li class="nav-item btn-deconnexion">
                <a class="nav-link" href="{{ path('app_register') }}">Nouveau</a>
            </li>
            {% endif %}
            <!--DECONNEXION-->
            {% if app.user %}
                <p class="login-name">{{ app.user.email }}</p>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('my_profil') }}">Mon compte</a>
                </li>
                <li class="nav-item btn-deconnexion">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            {% else %}
            <!--CONNEXION-->
                <li class="nav-item btn-connexion">
                    <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>